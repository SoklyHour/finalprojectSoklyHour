from django.apps import AppConfig  # Import AppConfig from django.apps to define configuration for the app

# Define the configuration for the 'weatherApp' app
class WeatherappConfig(AppConfig):
    name = 'weatherApp'  # Specify the name of the application as 'weatherApp'
